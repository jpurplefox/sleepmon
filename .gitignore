# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Env / secrets
.env
.env.*
!.env.example

# Node / frontend
node_modules/
frontend/dist/
*.local
*.tsbuildinfo
# Artefactos si alguien corre `tsc -b` sobre la config de Vite
frontend/vite.config.js
frontend/vite.config.d.ts

# OS
.DS_Store

# Claude Code (local-only overrides; keep shared config tracked)
.claude/settings.local.json
# The `build/` skill collides with the Python `build/` ignore above — keep it tracked
!.claude/skills/build/

# Coverage
.coverage
htmlcov/
